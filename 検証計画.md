# Google Directions API 技術検証計画書

## 1. 検証の目的

このプロジェクトは、ハッカソンで開発予定の「執事アプリ」の技術的実現可能性を検証することを目的としています。

### 執事アプリとは
ユーザーの移動予定を把握し、リアルタイムの交通情報を考慮した上で、**「○○分前にXXを出発すれば間に合います！」** とリマインドする機能を提供するアプリケーションです。

### 検証したい技術要件
1. **最短経路と所要時間の正確な取得**
2. **複数の交通手段の比較**（車、徒歩、公共交通機関）
3. **リアルタイム交通情報の反映**
4. **代替ルートの提案**
5. **API利用コストと制限の把握**
6. **代替技術の評価**（Google以外の選択肢）

---

## 2. 重要な技術的発見

### 2.1 Routes API への移行推奨 ⚠️

**Google Directions API（従来）は2025年3月1日からLegacyステータスに移行しました。**
新規開発には **Routes API** の使用が強く推奨されています。

#### Routes APIの優位性

| 項目 | Directions API (Legacy) | Routes API (推奨) |
|------|------------------------|-------------------|
| ステータス | Legacy (2025年3月～) | 一般提供中 |
| パフォーマンス | 標準 | 高速化 |
| 無料枠 | 月間100,000リクエスト | 月間625,000リクエスト |
| 新機能 | なし | 通行料金、2輪車、TRAFFIC_AWARE_OPTIMAL |
| リクエスト方式 | GET (URLパラメータ) | POST (JSONボディ) |

**結論：** このプロジェクトでは **Routes API** を採用します。

### 2.2 技術スタックの選定

#### バックエンド: FastAPI
- **非同期処理のネイティブサポート** - 外部API呼び出しに最適
- **高パフォーマンス** - Flaskの5-7倍（15,000-20,000 RPS）
- **自動ドキュメント生成** - OpenAPI/Swagger
- **型安全性** - Pydantic、TypeScriptとの相性良好

#### フロントエンド: React + Vite + TypeScript
- **@vis.gl/react-google-maps** - Google公式サポートの最新Reactライブラリ（2024年リリース）
- **完全にReactive** - Hooksベース、TypeScript完全対応

---

## 3. プロジェクト構成

```
demo-project-Google-Directions-API/
├── backend/                        # FastAPIバックエンド
│   ├── .env                        # APIキー（.gitignore対象）
│   ├── requirements.txt            # 依存パッケージ
│   ├── app/
│   │   ├── main.py                 # FastAPIエントリーポイント
│   │   ├── config.py               # 環境変数管理
│   │   ├── api/
│   │   │   └── directions.py      # Routes APIエンドポイント
│   │   ├── services/
│   │   │   └── google_maps.py     # Routes API統合（コアロジック）
│   │   ├── models/
│   │   │   ├── request.py          # Pydanticリクエストモデル
│   │   │   └── response.py         # Pydanticレスポンスモデル
│   │   └── utils/
│   │       └── helpers.py
│   └── tests/
│       └── test_api.py
│
├── frontend/                       # React + Vite + TypeScript
│   ├── .env                        # APIキー（.gitignore対象）
│   ├── package.json
│   ├── vite.config.ts
│   ├── src/
│   │   ├── main.tsx
│   │   ├── App.tsx
│   │   ├── components/
│   │   │   ├── Map/
│   │   │   │   ├── MapContainer.tsx      # 地図表示
│   │   │   │   └── RouteDisplay.tsx      # ルート描画
│   │   │   ├── Form/
│   │   │   │   ├── RouteSearchForm.tsx   # 検索フォーム
│   │   │   │   └── TransportModeSelector.tsx
│   │   │   └── Result/
│   │   │       ├── RouteInfoCard.tsx     # 結果表示
│   │   │       ├── AlternativeRoutes.tsx # 代替ルート
│   │   │       └── DepartureReminder.tsx # 出発時刻リマインダー
│   │   ├── hooks/
│   │   │   └── useDirections.ts    # API呼び出しロジック
│   │   ├── services/
│   │   │   └── api.ts              # バックエンド通信
│   │   └── types/
│   │       └── index.ts            # TypeScript型定義
│   └── tests/
│
├── docs/
│   ├── api-comparison.md           # API比較レポート
│   └── setup-guide.md              # セットアップガイド
│
├── .gitignore
├── 検証計画.md                      # 本ドキュメント
└── README.md
```

---

## 4. 技術スタック詳細

### 4.1 バックエンド

| 技術 | バージョン | 用途 |
|------|-----------|------|
| Python | 3.11+ | プログラミング言語 |
| FastAPI | 0.115+ | Webフレームワーク |
| httpx | 0.27+ | 非同期HTTPクライアント（Routes API呼び出し） |
| pydantic | 2.9+ | データバリデーション |
| python-dotenv | 1.0+ | 環境変数管理 |
| uvicorn | 0.32+ | ASGIサーバー |
| pytest | 8.3+ | テストフレームワーク |

#### requirements.txt
```txt
fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.0
pydantic==2.9.0
pydantic-settings==2.5.2
python-dotenv==1.0.0

# 開発用
pytest==8.3.0
pytest-asyncio==0.24.0
black==24.8.0
ruff==0.7.0
```

### 4.2 フロントエンド

| 技術 | バージョン | 用途 |
|------|-----------|------|
| TypeScript | 5.6+ | プログラミング言語 |
| React | 18.3+ | UIライブラリ |
| Vite | 6.0+ | ビルドツール |
| @vis.gl/react-google-maps | 1.3+ | Google Maps統合 |
| axios | 1.7+ | HTTPクライアント |
| date-fns | 4.1+ | 日時計算 |
| Tailwind CSS | 3.4+ | スタイリング |
| Radix UI | - | アクセシブルUIコンポーネント |
| vitest | 2.1+ | テストフレームワーク |

#### package.json（主要な依存関係）
```json
{
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@vis.gl/react-google-maps": "^1.3.0",
    "axios": "^1.7.0",
    "date-fns": "^4.1.0",
    "lucide-react": "^0.460.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "tailwindcss": "^3.4.0",
    "vitest": "^2.1.0"
  }
}
```

### 4.3 API & サービス

| API | 用途 | 備考 |
|-----|------|------|
| Google Routes API | 経路検索、所要時間計算 | メインAPI |
| Google Maps JavaScript API | 地図表示 | フロントエンド |
| Google Geocoding API | 住所→座標変換 | 補助API |

---

## 5. Google Routes API 機能検証

### 5.1 検証したい機能の実現可能性

| 機能 | 対応状況 | 実装方法 | 備考 |
|------|---------|---------|------|
| ✅ 最短経路と所要時間 | 完全対応 | `duration`, `distanceMeters`フィールド | 必須機能 |
| ✅ 交通手段の比較 | 完全対応 | `travelMode`: DRIVE, WALK, TRANSIT, BICYCLE, TWO_WHEELER | 5種類対応 |
| ✅ リアルタイム交通情報 | 対応 | `routingPreference: "TRAFFIC_AWARE"` | 精度検証必要 |
| ✅ 代替ルート | 対応 | `computeAlternativeRoutes: true` | 最大3ルート |

### 5.2 Routes API リクエスト例

```python
# backend/app/services/google_maps.py
async def compute_route(
    self,
    origin: Dict[str, float],
    destination: Dict[str, float],
    travel_mode: str = "DRIVE",
    compute_alternative_routes: bool = False
) -> Dict:
    """Routes APIを使用して経路を計算"""
    headers = {
        "Content-Type": "application/json",
        "X-Goog-Api-Key": self.api_key,
        "X-Goog-FieldMask": "routes.duration,routes.distanceMeters,routes.polyline,routes.legs"
    }

    payload = {
        "origin": {
            "location": {
                "latLng": {
                    "latitude": origin["lat"],
                    "longitude": origin["lng"]
                }
            }
        },
        "destination": {
            "location": {
                "latLng": {
                    "latitude": destination["lat"],
                    "longitude": destination["lng"]
                }
            }
        },
        "travelMode": travel_mode,
        "computeAlternativeRoutes": compute_alternative_routes,
        "routingPreference": "TRAFFIC_AWARE"  # リアルタイムトラフィック
    }

    async with httpx.AsyncClient() as client:
        response = await client.post(
            "https://routes.googleapis.com/directions/v2:computeRoutes",
            json=payload,
            headers=headers
        )
        response.raise_for_status()
        return response.json()
```

### 5.3 料金体系（2025年3月1日改定）

**重要：** 従来の「月額$200の一律クレジット」は2025年2月28日で終了し、SKUごとの無料枠に変更されました。

#### Routes API (Compute Routes)

| カテゴリ | 無料枠/月 | 超過後の料金 | 備考 |
|---------|-----------|------------|------|
| **Essentials** | 10,000リクエスト | $4.00～$3.00 / 1,000 | 基本的な経路検索 |
| **Pro** | 5,000リクエスト | $8.00～$10.00 / 1,000 | TRAFFIC_AWARE対応（本プロジェクトで使用） |
| **Enterprise** | 1,000リクエスト | $12.00～$15.00 / 1,000 | 2輪車ルーティング等 |

#### Maps JavaScript API (Dynamic Maps)

| 項目 | 無料枠/月 | 超過後の料金 |
|------|-----------|------------|
| Dynamic Maps | 10,000マップロード | $7.00 / 1,000（初期段階） |

#### Geocoding API

| 項目 | 無料枠/月 | 超過後の料金 |
|------|-----------|------------|
| Geocoding | 10,000リクエスト | $5.00 / 1,000 |

**コスト試算例（本プロジェクト）:**

**開発・ハッカソン検証（月間数百リクエスト）:**
- Routes API (Pro): 500リクエスト → 無料枠内 ✅
- Geocoding API: 1,000リクエスト → 無料枠内 ✅
- Maps JavaScript: 500ロード → 無料枠内 ✅
- **合計: $0/月**

**小規模運用（MAU 100人、1人あたり月20回検索）:**
- 月間リクエスト: 2,000回
- 1回の検索 = Routes API 1回 + Geocoding API 2回 + Maps 1回
- Routes API (Pro): 2,000リクエスト → 無料枠内 ✅
- Geocoding API: 4,000リクエスト → 無料枠内 ✅
- Maps JavaScript: 2,000ロード → 無料枠内 ✅
- **合計: $0/月** ✅

**中規模運用（MAU 1,000人、1人あたり月10回検索）:**
- 月間リクエスト: 10,000回
- Routes API (Pro): 10,000リクエスト
  - 無料: 5,000
  - 課金: 5,000 × $8/1,000 = $40
- Geocoding API: 20,000リクエスト
  - 無料: 10,000
  - 課金: 10,000 × $5/1,000 = $50
- Maps JavaScript: 10,000ロード → 無料枠内 ✅
- **合計: $90/月**

**本格運用（MAU 5,000人、1人あたり月10回検索）:**
- 月間リクエスト: 50,000回
- Routes API (Pro): 50,000リクエスト
  - 課金: 45,000 × $8～$6/1,000 = $300～$270
- Geocoding API: 100,000リクエスト
  - 課金: 90,000 × $5～$4/1,000 = $450～$360
- Maps JavaScript: 50,000ロード
  - 課金: 40,000 × $7～$5.6/1,000 = $280～$224
- **合計: $1,030～$854/月**（ボリュームディスカウント込み）

### 5.4 レート制限

- **デフォルト:** 100 QPS（Queries Per Second）
- **上限緩和:** 必要に応じて申請可能
- **推奨対策:** エラーハンドリング、Retry機能実装

---

## 6. Google Cloud Platform セットアップ手順

### 6.1 プロジェクト作成

1. https://console.cloud.google.com/ にアクセス
2. 「プロジェクトを作成」をクリック
3. プロジェクト名を入力（例：`butler-app-directions-demo`）
4. 「作成」をクリック

### 6.2 課金設定

1. 左メニュー「課金」→「請求先アカウントをリンク」
2. クレジットカード情報を登録
3. **無料枠の確認**
   - 2025年3月1日以降、SKUごとの無料リクエスト数が適用される
   - Routes API (Pro): 5,000リクエスト/月
   - Maps JavaScript API: 10,000マップロード/月
   - Geocoding API: 10,000リクエスト/月
4. 課金アラートを設定（推奨：$20で通知）

### 6.3 APIの有効化

以下の3つのAPIを有効化します：

1. **Routes API**
   - 「APIとサービス」→「ライブラリ」
   - 「Routes API」を検索
   - 「有効にする」をクリック

2. **Maps JavaScript API**
   - 同様に検索して有効化
   - フロントエンドの地図表示に使用

3. **Geocoding API**
   - 同様に検索して有効化
   - 住所→座標変換に使用

### 6.4 APIキーの作成

2つのAPIキーを作成します：

#### バックエンド用APIキー
1. 「APIとサービス」→「認証情報」
2. 「認証情報を作成」→「APIキー」
3. キーが生成されたら「キーを制限」をクリック

**制限設定：**
- **名前:** `backend-routes-api-key`
- **アプリケーションの制限:** IPアドレス（本番環境）/ なし（開発時）
- **APIの制限:** Routes API, Geocoding API のみ
- 「保存」をクリック

#### フロントエンド用APIキー
1. 再度「認証情報を作成」→「APIキー」
2. キーが生成されたら「キーを制限」をクリック

**制限設定：**
- **名前:** `frontend-maps-api-key`
- **アプリケーションの制限:** HTTPリファラー
  - 開発時：`localhost:5173/*`, `127.0.0.1:5173/*`
  - 本番時：`yourdomain.com/*`
- **APIの制限:** Maps JavaScript API のみ
- 「保存」をクリック

### 6.5 環境変数設定

#### backend/.env
```bash
GOOGLE_MAPS_API_KEY=AIzaSy... (バックエンド用キー)
```

#### frontend/.env
```bash
VITE_GOOGLE_MAPS_API_KEY=AIzaSy... (フロントエンド用キー)
```

#### .gitignoreに追加（重要）
```gitignore
# 環境変数ファイル
.env
.env.local

# Node modules
node_modules/

# Python virtual environment
venv/
__pycache__/
*.pyc
```

#### .env.exampleを作成（テンプレート）
```bash
# backend/.env.example
GOOGLE_MAPS_API_KEY=your_backend_api_key_here

# frontend/.env.example
VITE_GOOGLE_MAPS_API_KEY=your_frontend_api_key_here
```

---

## 7. 実装フェーズ計画

### Phase 1: MVP - 基本的な経路検索 【1-2日】
**優先度：最高 🔴**

#### 目標
基本的な経路検索と所要時間計算を実装し、Routes APIの動作を確認する。

#### バックエンド実装内容
1. ✅ FastAPIプロジェクト初期化
   ```bash
   mkdir -p backend/app/{api,services,models,utils} backend/tests
   cd backend
   python -m venv venv
   source venv/bin/activate
   pip install fastapi uvicorn httpx pydantic pydantic-settings python-dotenv
   ```

2. ✅ `config.py` - 環境変数管理
   ```python
   from pydantic_settings import BaseSettings

   class Settings(BaseSettings):
       GOOGLE_MAPS_API_KEY: str

       class Config:
           env_file = ".env"

   settings = Settings()
   ```

3. ✅ `services/google_maps.py` - Routes API統合（前述のコード参照）

4. ✅ `api/directions.py` - エンドポイント実装
   ```python
   @router.post("/routes")
   async def calculate_route(request: RouteRequest):
       # 実装コード
       pass
   ```

5. ✅ `main.py` - FastAPIアプリ初期化、CORSミドルウェア設定

#### フロントエンド実装内容
1. ✅ プロジェクト初期化
   ```bash
   npm create vite@latest frontend -- --template react-ts
   cd frontend
   npm install @vis.gl/react-google-maps axios date-fns
   npm install -D tailwindcss autoprefixer postcss
   npx tailwindcss init -p
   ```

2. ✅ `components/Form/RouteSearchForm.tsx` - 入力フォーム
   - 出発地入力フィールド
   - 目的地入力フィールド
   - 検索ボタン

3. ✅ `hooks/useDirections.ts` - API呼び出しロジック
   ```typescript
   export function useDirections() {
     const [isLoading, setIsLoading] = useState(false);
     const [routeData, setRouteData] = useState(null);

     const calculateRoute = async (request) => {
       const response = await axios.post('/api/v1/routes', request);
       setRouteData(response.data);
     };

     return { calculateRoute, isLoading, routeData };
   }
   ```

4. ✅ 基本的な結果表示（所要時間、距離）

#### 検証項目
- [ ] Routes APIが正常に動作するか
- [ ] 所要時間の精度（実際の所要時間と比較）
- [ ] レスポンス速度（通常1秒以内が目標）
- [ ] エラーハンドリング（無効な住所、APIキー誤りなど）

#### 成果物
- 動作するプロトタイプ
- API疎通確認完了

---

### Phase 2: 地図表示と視覚化 【1-2日】
**優先度：高 🟠**

#### 目標
ユーザーフレンドリーな地図表示とルート可視化。

#### バックエンド実装内容
1. ✅ Geocoding API統合（住所文字列→座標変換）
2. ✅ レスポンスにポリライン情報を含める
3. ✅ エラーハンドリング強化

#### フロントエンド実装内容
1. ✅ `@vis.gl/react-google-maps` 統合
   ```tsx
   import { APIProvider, Map, Marker } from '@vis.gl/react-google-maps';

   export function MapContainer({ origin, destination, route }) {
     return (
       <APIProvider apiKey={import.meta.env.VITE_GOOGLE_MAPS_API_KEY}>
         <Map defaultCenter={origin} defaultZoom={12}>
           <Marker position={origin} label="出発" />
           <Marker position={destination} label="到着" />
         </Map>
       </APIProvider>
     );
   }
   ```

2. ✅ `components/Map/MapContainer.tsx` - 地図表示
3. ✅ `components/Map/RouteDisplay.tsx` - ポリライン描画
4. ✅ Tailwind CSSでUI改善

#### 検証項目
- [ ] 地図のレンダリング速度
- [ ] ポリラインの精度と視認性
- [ ] モバイルでの表示（レスポンシブデザイン）
- [ ] 地図の操作性（ズーム、パン）

#### 成果物
- 視覚的に分かりやすいUIデモ
- 地図上でルートが確認できる

---

### Phase 3: 執事機能の実装 【2-3日】
**優先度：最高 🔴（ハッカソンのコア機能）**

#### 目標
**「○○分前にXXを出発すれば間に合います！」** 機能の実装。

#### バックエンド実装内容
1. ✅ リクエストモデルに到着希望時刻パラメータ追加
   ```python
   class RouteRequest(BaseModel):
       origin: str
       destination: str
       travel_mode: str = "DRIVE"
       desired_arrival_time: datetime  # 追加
   ```

2. ✅ リアルタイムトラフィック考慮
   ```python
   payload = {
       # ...
       "routingPreference": "TRAFFIC_AWARE",  # リアルタイム交通情報
       "departureTime": datetime.now().isoformat()  # 現在時刻から出発
   }
   ```

3. ✅ 出発時刻計算ロジック
   ```python
   duration_seconds = route_data['routes'][0]['duration']
   arrival_time = request.desired_arrival_time
   recommended_departure = arrival_time - timedelta(seconds=duration_seconds)
   ```

4. ✅ レスポンスに推奨出発時刻を含める

#### フロントエンド実装内容
1. ✅ 到着希望時刻入力フィールド
   ```tsx
   <input
     type="datetime-local"
     value={arrivalTime}
     onChange={(e) => setArrivalTime(e.target.value)}
   />
   ```

2. ✅ `components/Result/DepartureReminder.tsx` - リマインダーUI
   ```tsx
   export function DepartureReminder({ departureTime, currentTime }) {
     const minutesUntilDeparture = differenceInMinutes(departureTime, currentTime);

     return (
       <div className="reminder-card">
         {minutesUntilDeparture > 0 ? (
           <p>あと{minutesUntilDeparture}分で出発してください！</p>
         ) : (
           <p className="warning">すでに出発時刻を過ぎています！</p>
         )}
       </div>
     );
   }
   ```

3. ✅ カウントダウン表示（リアルタイム更新）
4. ✅ 通知UI（緊急度に応じた色分け）

#### 検証項目
- [ ] トラフィック情報の精度（平日昼間 vs 夕方ラッシュ時で比較）
- [ ] 時間計算の正確性
- [ ] リアルタイム更新の頻度（5分ごと？10分ごと？）
- [ ] バッテリー消費量（定期的なAPI呼び出しの影響）

#### 成果物
- **執事アプリのコア機能デモ**
- ハッカソンでのプレゼンテーション可能な状態

**Phase 1-3で基本的な執事機能が完成します（ハッカソン向けミニマム実装）**

---

### Phase 4: 交通手段比較 【1-2日】
**優先度：中 🟡**

#### 目標
車・徒歩・公共交通機関の比較表示。

#### バックエンド実装内容
1. ✅ 複数の `travelMode` で並列リクエスト
   ```python
   travel_modes = ["DRIVE", "WALK", "TRANSIT", "BICYCLE"]

   tasks = [
       compute_route(origin, destination, mode)
       for mode in travel_modes
   ]

   results = await asyncio.gather(*tasks)
   ```

2. ✅ 結果を配列で返す（比較しやすい形式）

#### フロントエンド実装内容
1. ✅ `components/Form/TransportModeSelector.tsx` - モード選択
2. ✅ 比較表示UI
   - 所要時間
   - 距離
   - 推定コスト（ガソリン代、運賃など）
3. ✅ 最適な交通手段のハイライト

#### 検証項目
- [ ] 各交通手段の精度
- [ ] 公共交通機関の時刻表精度（日本の電車・バスデータ）
- [ ] 並列リクエストのパフォーマンス

---

### Phase 5: 代替ルート 【1-2日】
**優先度：低 🟢**

#### 目標
複数ルートオプションの提供。

#### 実装内容
1. ✅ `computeAlternativeRoutes: true` 設定
2. ✅ `components/Result/AlternativeRoutes.tsx` - 一覧表示
3. ✅ 通行料金情報表示

#### 検証項目
- [ ] 代替ルートの質（実用的か）
- [ ] 通行料金の精度

---

### Phase 6: 代替技術検証（オプション）

#### 目標
Mapboxとの比較評価。

#### 実装内容
1. ✅ Mapbox Directions API統合
2. ✅ 結果比較（精度、速度、コスト）
3. ✅ `docs/api-comparison.md` にレポート作成

---

## 8. 代替技術の評価

### 8.1 主要な代替サービス比較

| サービス | 無料枠 | 価格 | 日本精度 | 公共交通 | 推奨度 |
|---------|--------|------|---------|---------|--------|
| **Google Routes API** | $300クレジット | $5/1,000 | ★★★★★ | ★★★★★ | ★★★★★ |
| **Mapbox Directions** | 100,000リクエスト/月 | $0.40/1,000 | ★★★☆☆ | ★★☆☆☆ | ★★★★☆ |
| **HERE Routing** | 250,000トランザクション/月 | $1.00/1,000 | ★★★★☆ | ★★★☆☆ | ★★★☆☆ |
| **OpenRouteService** | 2,000リクエスト/日（無料） | オープンソース | ★★☆☆☆ | ★☆☆☆☆ | ★★☆☆☆ |

### 8.2 Google Routes API の優位性

**日本国内での使用に最適な理由：**
1. ✅ 日本の公共交通機関データが最も充実（電車、バス、地下鉄）
2. ✅ リアルタイムトラフィック情報の精度が高い
3. ✅ 住所表記（漢字、ひらがな）の認識精度
4. ✅ 地図データの更新頻度が高い

### 8.3 Mapbox Directions API の検討

**メリット：**
- コスト効率が良い（Googleの1/12.5）
- 高い無料枠（100,000リクエスト/月）
- 地図のカスタマイズ性が高い

**デメリット：**
- 日本の公共交通機関データが弱い
- トラフィック情報がGoogleより劣る

**推奨用途：**
- 車/徒歩メインのルート検索
- グローバル展開を見据えたプロトタイプ
- デザイン重視のアプリ

### 8.4 技術選定の推奨戦略

#### ハッカソン技術検証フェーズ
- **Google Routes API** をメイン実装
- Mapboxは時間があれば比較検証

#### 本番環境（執事アプリ）
- **日本国内特化：** Google Routes API（精度優先）
- **初期MVP：** Mapbox無料枠でスタート、成長後にGoogle移行も検討
- **グローバル展開：** Mapbox（コスト優先）

---

## 9. Critical Files（実装で特に重要なファイル）

### 9.1 バックエンド

#### `/backend/app/services/google_maps.py`
**役割：** Routes API統合のコアロジック

**実装内容：**
- `GoogleMapsService` クラス
- `compute_route()` メソッド - Routes API呼び出し
- 非同期HTTPリクエスト（httpx使用）
- エラーハンドリング（レート制限、無効なリクエスト）
- レスポンスのパース

#### `/backend/app/api/directions.py`
**役割：** FastAPIエンドポイント定義

**実装内容：**
- `/api/v1/routes` POSTエンドポイント
- リクエスト/レスポンスバリデーション（Pydantic）
- 出発時刻計算ロジック
- CORSヘッダー設定

#### `/backend/app/config.py`
**役割：** 環境変数管理

**実装内容：**
- `Settings` クラス（Pydantic BaseSettings）
- `GOOGLE_MAPS_API_KEY` 読み込み
- `.env`ファイル連携

### 9.2 フロントエンド

#### `/frontend/src/hooks/useDirections.ts`
**役割：** バックエンドAPI呼び出しロジック

**実装内容：**
- `calculateRoute()` 関数
- 状態管理（loading, error, data）
- axios設定
- エラーハンドリング

#### `/frontend/src/components/Map/MapContainer.tsx`
**役割：** 地図表示

**実装内容：**
- `@vis.gl/react-google-maps` の使用パターン
- `APIProvider`, `Map`, `Marker` コンポーネント
- ポリライン描画
- レスポンシブデザイン

#### `/frontend/src/components/Result/DepartureReminder.tsx`
**役割：** 執事機能のUI

**実装内容：**
- 推奨出発時刻表示
- カウントダウンロジック（リアルタイム更新）
- 通知UI（緊急度に応じた色分け）
- `date-fns` で日時計算

---

## 10. セキュリティとベストプラクティス

### 10.1 APIキー管理

| 項目 | 対策 |
|------|------|
| 環境変数化 | `.env`ファイルで管理、`.gitignore`に追加（必須） |
| キー制限 | HTTPリファラー/IPアドレス制限 |
| API制限 | 使用するAPIのみ許可 |
| モニタリング | Google Cloud Consoleで使用量監視 |
| ローテーション | 定期的にキーを再生成 |

### 10.2 エラーハンドリング

```python
# バックエンド例
try:
    route_data = await maps_service.compute_route(...)
except httpx.HTTPStatusError as e:
    if e.response.status_code == 429:
        # レート制限
        raise HTTPException(status_code=429, detail="リクエスト制限に達しました")
    elif e.response.status_code == 400:
        # 無効なリクエスト
        raise HTTPException(status_code=400, detail="無効な住所です")
    else:
        raise HTTPException(status_code=500, detail="APIエラーが発生しました")
```

### 10.3 パフォーマンス最適化

| 技術 | 効果 | 実装方法 |
|------|------|---------|
| Field Mask | API料金削減 | 必要なデータのみリクエスト |
| キャッシング | レスポンス高速化 | 同じ経路の重複リクエスト削減 |
| 非同期処理 | スループット向上 | FastAPIのasync/await活用 |
| 並列リクエスト | 複数交通手段比較の高速化 | `asyncio.gather()` 使用 |

---

## 11. 想定されるリスクと対策

| リスク | 影響度 | 対策 |
|--------|--------|------|
| APIコストの急増 | 高 | Field Mask使用、キャッシング、無料枠監視、予算アラート設定 |
| レート制限到達 | 中 | Retry機能、エラーハンドリング、QPS上限緩和申請 |
| 公共交通データ不足 | 中 | Google優先（日本では最も充実） |
| APIキー漏洩 | 高 | 環境変数管理、.gitignore、APIキー制限設定、定期ローテーション |
| リアルタイム精度低下 | 中 | TRAFFIC_AWARE_OPTIMAL使用、定期的な再計算 |
| トラフィック予測誤差 | 中 | 余裕時間を追加（所要時間×1.2など） |

---

## 12. 検証の成功基準

### 12.1 技術的成功基準

- [x] Routes APIで正確な経路と所要時間を取得できる
- [ ] リアルタイムトラフィック情報が反映される（精度±10%以内）
- [ ] 推奨出発時刻が適切に計算される
- [ ] 複数の交通手段を比較できる
- [ ] レスポンス速度が1秒以内
- [ ] エラーハンドリングが適切に機能する

### 12.2 ビジネス的成功基準

- [ ] 執事アプリの実現可能性が確認できる
- [ ] コスト見積もりができる
  - 1日あたりのリクエスト数
  - 月間予想コスト
- [ ] ハッカソンでデモ可能なプロトタイプが完成
- [ ] 技術選定の判断材料が揃う（Google vs Mapbox）

### 12.3 ユーザビリティ基準

- [ ] 直感的なUI（非技術者でも使える）
- [ ] 視覚的に分かりやすい地図表示
- [ ] モバイルでも快適に操作できる
- [ ] エラーメッセージが分かりやすい

---

## 13. 実装の優先順位と所要時間

### 13.1 ハッカソン向けミニマム実装（推奨）
**Phase 1 → Phase 2 → Phase 3**

| フェーズ | 所要時間 | 累計 |
|---------|---------|------|
| Phase 1: MVP | 1-2日 | 1-2日 |
| Phase 2: 地図表示 | 1-2日 | 2-4日 |
| Phase 3: 執事機能 | 2-3日 | 4-7日 |

**この3フェーズで執事アプリのコア機能が完成します。**

### 13.2 フル機能実装
**Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5**

| フェーズ | 所要時間 | 累計 |
|---------|---------|------|
| Phase 1-3 | 4-7日 | 4-7日 |
| Phase 4: 交通手段比較 | 1-2日 | 5-9日 |
| Phase 5: 代替ルート | 1-2日 | 6-11日 |

### 13.3 代替技術検証含む
上記 + Phase 6: 代替技術検証（+2-3日）

**合計：8-14日**

---

## 14. 次のステップ（アクションアイテム）

### 14.1 即座に実行すべきこと

#### ステップ1: Google Cloud Platformセットアップ（30分）
- [ ] プロジェクト作成
- [ ] 課金設定（$300クレジット取得）
- [ ] Routes API, Maps JavaScript API, Geocoding API 有効化
- [ ] APIキー2つ作成（バックエンド用、フロントエンド用）
- [ ] APIキー制限設定

#### ステップ2: プロジェクト初期化（1時間）
```bash
# バックエンド
mkdir -p backend/app/{api,services,models,utils} backend/tests
cd backend
python -m venv venv
source venv/bin/activate
pip install fastapi uvicorn httpx pydantic pydantic-settings python-dotenv

# フロントエンド
cd ../
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install @vis.gl/react-google-maps axios date-fns
npm install -D tailwindcss autoprefixer postcss
npx tailwindcss init -p
```

#### ステップ3: 環境変数設定（10分）
- [ ] `backend/.env` 作成、APIキー設定
- [ ] `frontend/.env` 作成、APIキー設定
- [ ] `.gitignore` に `.env` 追加
- [ ] `.env.example` 作成

#### ステップ4: Phase 1実装開始（1-2日）
- [ ] バックエンド: `main.py` と基本エンドポイント
- [ ] フロントエンド: 入力フォームと結果表示

### 14.2 ドキュメント作成推奨

- [ ] `docs/setup-guide.md` - 環境構築手順の詳細版
- [ ] `docs/api-comparison.md` - Google vs Mapbox 比較レポート
- [ ] `README.md` - プロジェクト概要とクイックスタートガイド

### 14.3 ハッカソン準備

- [ ] Phase 1-3の実装完了
- [ ] デモシナリオ作成
  - 例：「渋谷から東京駅まで、15:00到着希望」
  - トラフィック情報を考慮した推奨出発時刻の提示
- [ ] プレゼンテーション資料作成
- [ ] 動画デモ録画（ネットワーク障害対策）

---

## 15. まとめ

### 15.1 このプロジェクトで達成できること

1. ✅ **技術検証完了** - Google Routes APIの機能と制限を理解
2. ✅ **プロトタイプ完成** - デモ可能な執事アプリ
3. ✅ **コスト分析** - Googleと代替サービスの比較データ
4. ✅ **スケーラブルな設計** - 本番環境への拡張性

### 15.2 執事アプリの実現可能性

**結論：技術的に実現可能** ✅

| 機能 | 実現可能性 | 備考 |
|------|-----------|------|
| 移動時間計算 | ✅ 可能 | Routes APIで高精度 |
| リアルタイム交通情報 | ✅ 可能 | TRAFFIC_AWARE対応 |
| 出発時刻リマインダー | ✅ 可能 | 本プロジェクトで実装 |
| 複数交通手段比較 | ✅ 可能 | 並列リクエストで実現 |
| 通知機能 | ⚠️ 別途実装必要 | プッシュ通知、メール等 |

### 15.3 推奨される次のステップ

1. **Phase 1-3を確実に完成させる**（ハッカソン最優先）
2. **コスト監視体制を構築**（予算オーバー防止）
3. **ユーザーテスト実施**（実際の移動で精度確認）
4. **代替技術の評価**（Mapboxで比較データ取得）

---

## 付録A: よくある質問（FAQ）

### Q1: APIキーが漏洩した場合の対処法は？
**A:** すぐにGoogle Cloud Consoleでキーを無効化し、新しいキーを生成してください。GitHubにpushした場合は、git historyからも削除が必要です。

### Q2: 無料枠を超えた場合の警告は？
**A:** Google Cloud Consoleで課金アラートを設定してください（推奨：$50で通知）。

### Q3: リアルタイムトラフィック情報の精度は？
**A:** 平日ラッシュ時で±5-10分程度の誤差が想定されます。余裕時間を追加することを推奨します。

### Q4: 公共交通機関の時刻表は常に最新？
**A:** Googleは主要な交通機関と提携していますが、運休情報などはリアルタイムではない場合があります。

### Q5: 海外でも使える？
**A:** はい。ただし、国によって精度が異なります。日本は精度が高い地域の一つです。

---

## 付録B: 参考リンク

### Google Maps Platform
- [Routes API公式ドキュメント](https://developers.google.com/maps/documentation/routes)
- [料金体系](https://developers.google.com/maps/documentation/routes/usage-and-billing)
- [マイグレーションガイド](https://developers.google.com/maps/documentation/routes/migrate-routes)

### 開発ツール
- [FastAPI公式ドキュメント](https://fastapi.tiangolo.com/)
- [@vis.gl/react-google-maps](https://visgl.github.io/react-google-maps/)
- [Vite公式ドキュメント](https://vitejs.dev/)

### 代替サービス
- [Mapbox Directions API](https://docs.mapbox.com/api/navigation/directions/)
- [HERE Routing API](https://developer.here.com/documentation/routing-api/dev_guide/index.html)

---

**このドキュメントは、技術検証プロジェクトの完全なガイドラインです。Phase 1から順番に実装を進めてください。**

**最終更新日：2026年2月14日**
